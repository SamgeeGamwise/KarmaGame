# Maps Folder

Put your Tiled assets here:
- `*.tmx` map files
- external tilesets `*.tsx`
- referenced textures (PNG)

Default runtime load target in Sandbox is `Maps/StartTown` (asset name, no extension).

After adding files, include them in `Content.mgcb` via MGCB Editor.
